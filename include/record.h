#ifndef RECORD_H
#define RECORD_H

// data model representing one input record
// id    : record identifier
// value : value
struct Record {
    long long id;
    long long value;
};

#endif // RECORD_H